# AI Image Workshop v1 Archive

Status: completed and superseded by the authenticated React Router `0.2.0` application. This archive contains no active requirements.

- [x] Built the original Vite/React image generation UI, request adapter,
  validation, local configuration, redaction, tests, and responsive styling.
- [x] Used the v1 implementation as the visual and interaction baseline for the
  later v2 redesign.
- [x] Removed the anonymous browser-global credential model during the v2
  migration.

The current product contract is [redesign-requirements.md](../../redesign-requirements.md),
the current status is [PROGRESS.md](../../PROGRESS.md), and detailed v1 task
history remains available in Git.
