# 功能总清单

> 本文件是全站功能的"总账本"，AI 和人都以它为准判断"做完了没有"。
> 出处：Anthropic《Effective Harnesses for Long-Running Agents》的 feature list 机制。

## 使用规则（AI 必须遵守）

1. **只许改"状态"列，不许删除或改写任何条目。** 删除条目会导致功能漏测，是不可接受的行为。
2. 状态只有三种：`✅ 已验收`、`🚧 进行中`、`❌ 未实现/已损坏`。
3. 把条目标记为 `✅` 之前，必须亲自按"验收步骤"跑过对应测试/浏览器验证，并在当次 `docs/PROGRESS.md` 日志里贴出证据。
4. 新增功能必须先在 `tasks/` 建任务卡，验收后由 AI 把条目追加到本文件对应分类，编号顺延。
5. 每次新会话开始，先读本文件了解全局状态（见 CLAUDE.md 会话开始 SOP）。

## 状态说明

- 当前清单按 `0.2.0` 生产基线盘点（2026-07-24 接管会话建立）。
- 标注 `🚧` 的条目表示代码已实现但尚未在生产验收（如 v0.2.1 待部署功能）。

---

## 一、账户与登录

| 编号 | 功能 | 验收步骤 | 状态 |
|---|---|---|---|
| F-001 | 用户注册 | 1. 打开 /auth/register；2. 填邮箱密码提交；3. 注册成功并进入应用 | ✅ |
| F-002 | 用户登录/登出 | 1. /auth/login 登录；2. 顶栏显示已登录态；3. 登出后回到未登录态 | ✅ |
| F-003 | 忘记密码 | 1. /auth/forgot 提交邮箱；2. 按流程重置密码；3. 新密码可登录 | ✅ |
| F-004 | 登录态保持 | 1. 登录后刷新页面；2. 会话仍有效，不跳回登录页 | ✅ |

## 二、对话式生图（核心）

| 编号 | 功能 | 验收步骤 | 状态 |
|---|---|---|---|
| F-010 | 文生图（system 模式） | 1. 新建对话输入提示词；2. 提交出现任务卡；3. 任务终态成功且图片显示；4. 积分按规则扣减 | ✅ |
| F-011 | 图生图（上传参考图） | 1. 在 Composer 上传图片+提示词；2. 提交后成功；3. 结果图与参考图有关联 | ✅ |
| F-012 | 多任务状态追踪 | 1. 同会话提交多个任务；2. 各任务状态独立流转；3. 全部到达终态 | ✅ |
| F-013 | 失败不扣积分 | 1. 构造一个失败任务；2. 终态为 failed；3. 账本无对应扣费 | ✅ |
| F-014 | 对话结果图文字二次编辑 | 1. 成功卡片进入编辑态；2. 提交修改后生成新图；3. 来源关系正确显示 | 🚧 v0.2.1 已发布待部署 |

## 三、图片资产

| 编号 | 功能 | 验收步骤 | 状态 |
|---|---|---|---|
| F-020 | 查看历史生成图片 | 1. 打开 /assets；2. 历史图片列表正常加载；3. 点图可看大图 | ✅ |
| F-021 | 下载图片 | 1. 在资产或对话卡片点下载；2. 浏览器得到完整图片文件 | ✅ |
| F-022 | 图片持久化 | 1. 生成成功后重启/隔日再访问；2. 图片仍可打开（/media/* 可读） | ✅ |
| F-023 | 灵感库浏览与投稿 | 1. 打开 /inspiration 浏览；2. 投稿后在管理后台可见待审 | ✅ |

## 四、积分与计费

| 编号 | 功能 | 验收步骤 | 状态 |
|---|---|---|---|
| F-030 | 余额与账本展示 | 1. 打开 /billing 与 /account；2. 余额、批次、流水数字一致 | ✅ |
| F-031 | 成功才扣费 + FIFO | 1. 跑 npm run test:money；2. 扣费顺序与"成功才扣"用例全绿 | ✅（2026-07-25 真库复验：84/84 全绿） |
| F-032 | 兑换码充值 | 1. 后台生成兑换码；2. 用户侧兑换；3. 余额增加且审计有记录 | ✅ |
| F-033 | 余额不得为负 | 1. npm run test:money 全绿（含负余额防护用例） | ✅（2026-07-25 真库复验：84/84 全绿） |

## 五、Key 模式（system / custom）

| 编号 | 功能 | 验收步骤 | 状态 |
|---|---|---|---|
| F-040 | custom 模式生图零扣费 | 1. 保存自己的 Key 并切 custom；2. 生图成功；3. 本站积分不变 | ✅（2026-07-25 真库复验） |
| F-041 | custom 凭据终态销毁 | 1. custom 任务到终态；2. 数据库无残留明文/密文凭据；3. 日志无泄露 | ✅（2026-07-25 真库复验） |
| F-042 | custom 失败不回退 system | 1. custom 任务失败；2. 不触发 system 扣费、不换模式 | ✅（2026-07-25 真库复验） |

## 六、管理后台

| 编号 | 功能 | 验收步骤 | 状态 |
|---|---|---|---|
| F-050 | 管理员登录与 guard | 1. 未登录访问 /admin/* 被重定向；2. 管理员登录后可进入 | ✅ |
| F-051 | 用户/套餐/兑换码管理 | 1. 后台各页面增改生效；2. 敏感写入有审计记录 | ✅ |
| F-052 | 后台系统更新 | 1. /admin/system-update 检查版本；2. 启动更新后站点健康 | 🚧 v0.2.2 修复待发布 |
| F-053 | 灵感投稿审核 | 1. 后台审核投稿；2. 通过后前台可见 | ✅ |

## 七、基础设施

| 编号 | 功能 | 验收步骤 | 状态 |
|---|---|---|---|
| F-060 | 健康检查 | 1. 访问 /healthz 返回 204 | ✅ |
| F-061 | 备份与恢复 | 1. 按 deploy 手册执行备份；2. 校验和通过；3. 恢复演练成功 | ✅ |
| F-070 | 公开落地页 /welcome | 1. 未登录打开 /welcome 看到完整版块；2. 灵感画廊图片正常加载；3. CTA 跳转 /register；4. 桌面与手机宽度布局正常 | ✅（2026-07-25 截图验收） |
| F-071 | 落地页视觉重构：品牌+叙事+主题+照片墙 | 1. 导航/页脚显 one-image2、页脚注明"由 image-2 模型驱动"；2. 默认浅色，导航按钮可切深色并切回；3. 画廊图片铺满无白框、缝隙 12px、各列无整列空洞，hover 浮现标题+标签+提示词；4. 滚动时各版块有 Reveal 显现动效；5. 宽屏 5 列、窄屏自适应减列 | ✅（2026-07-25 截图验收：2560 宽屏/hover/1280 窄屏） |
| F-072 | 方案 B：根路径即门面 | 1. 未登录打开 / → 302 到 /welcome 看到落地页；2. 已登录打开 / → 直接进主对话页；3. 已登录打开 /welcome → 自动回 /；4. 未登录打开 /assets 等 → 照旧跳 /login?next= | ✅（2026-07-25 curl 验证双向跳转 + 截图） |
| F-073 | 后台手动配置未登录首页画廊 | 1. 后台「首页画廊」新增（上传/贴 URL）/编辑/上移下移/上下架/删除生效且有审计记录；2. 未登录 /welcome 只显示上架卡并按排序展示；3. 无上架卡时 /welcome 回退灵感库（再回退种子）；4. 上传图（landing/…）超 1h 不被孤儿清理误删（known-set 单测） | ✅（2026-07-25 浏览器实操 4 截图 + 14 条新测试全绿） |
